name: "main"
on:
    pull_request:
        types:
            - closed
        branches:
            - main
jobs:
    auto-tag:
        runs-on: ubuntu-latest
        steps:
            - uses: ./.github/workflows/auto-tag.yml

